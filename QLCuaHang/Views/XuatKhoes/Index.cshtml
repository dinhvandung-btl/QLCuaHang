@model IEnumerable<QLCuaHang.Models.XuatKho>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.KhachHang.TenKH)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NgayXuat)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LyDoXuat)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.KhachHang.TenKH)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NgayXuat)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LyDoXuat)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SoPhieuX }) |
            @Html.ActionLink("Details", "Details", new { id=item.SoPhieuX }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SoPhieuX })
        </td>
    </tr>
}

</table>
